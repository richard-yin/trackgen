{
  "name": "trackgen",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint:qml": "/usr/lib/qt6/bin/qmllint --import disable --unqualified disable trackgen/trackgen.qml 2>&1 | grep '^Error:' && exit 1 || true",
    "lint:js": "for f in trackgen/*.js; do node --check \"$f\"; done"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
