{
  "name": "trackgen",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint:qml": "/usr/lib/qt6/bin/qmllint --import disable trackgen/trackgen.qml"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
